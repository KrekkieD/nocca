'use strict';

describe('plugins/simpleMessageTransformer', function () {

    // it should start server

    // make sure we can change all relevant message types (clientReq, proxyReq, proxyRes, playbackRes)

    // it should stop the server

});
